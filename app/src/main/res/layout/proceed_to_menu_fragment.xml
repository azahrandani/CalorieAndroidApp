<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center_horizontal">

    <TextView
        android:id="@+id/textViewProceed"
        android:text="@string/lets_proceed"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/bmrGreen"
        android:textSize="30dp"
        android:gravity="center"/>

    <Button
        android:id="@+id/pick_menu_button"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:background="@color/bmrGreen"
        android:padding="10dp"
        android:text="@string/pick_my_menus"
        android:textSize="20dp"
        android:textColor="#000"
        android:layout_marginTop="20dp"
        app:layout_constraintTop_toBottomOf="@+id/textViewProceed"/>

</LinearLayout>